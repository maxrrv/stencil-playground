'use strict';

const playgroundReact = require('..');
const assert = require('assert').strict;

assert.strictEqual(playgroundReact(), 'Hello from playgroundReact');
console.info('playgroundReact tests passed');
